# こぐまハウス 業務支援システム

## 概要
保育園（こぐまハウス）の業務支援アプリケーション。SelectType予約システムからのCSVデータ取込、児童の登降園・ケア記録管理、東京都助成金対応の領収書PDF発行機能を提供します。

## 技術スタック
- **フロントエンド**: Streamlit
- **データベース**: SQLite3
- **PDF生成**: ReportLab + IPAexゴシックフォント
- **データ処理**: Pandas
- **祝日判定**: jpholiday

## ファイル構成
```
├── app.py              # メインStreamlitアプリ
├── config.py           # 料金設定マスタ（FEE_CONFIG辞書）
├── database.py         # SQLiteデータベース操作
├── pricing.py          # 料金計算ロジック（予約画面用）
├── calc_logic.py       # 料金計算シミュレーター専用ロジック（config.pyから読込）
├── pdf_generator.py    # PDF領収書生成
├── care_notes.py       # ケア記録・連絡帳テキスト生成
├── fonts/
│   └── ipaexg.ttf      # 日本語フォント
├── receipts/           # 生成PDF保存先
└── nursery.db          # SQLiteデータベース
```

## 料金設定の保守
料金改定時は `config.py` の `FEE_CONFIG` 辞書の数値を変更するだけで対応可能。
計算ロジック（calc_logic.py）の修正は不要。

## サービス区分・料金体系

### 1. 一時預かり保育
- **時間帯:**
  - 通常 (9:00-17:00): 平日 ¥2,000/h、土日祝 ¥3,200/h
  - 時間外 (7:00-9:00, 17:00-22:00): 平日 ¥2,800/h、土日祝 ¥4,000/h
- **施設利用料:** ¥550/回（任意）
- **兄弟割引:** △¥400/h
- **交通費:** なし
- **要件証明:** なし

### 2. ベビーシッター（施設型）
- **最低利用時間:** 2時間以上
- **時間帯:**
  - 通常 (9:00-17:00): 平日 ¥3,200/h、土日祝 ¥4,000/h
  - 時間外 (7:00-9:00, 17:00-22:00): 平日 ¥4,000/h、土日祝 ¥4,500/h
- **施設利用料:** ¥2,200/回
- **交通費:** なし
- **要件証明:** あり

### 3. 自宅ベビーシッター
- **最低利用時間:** 3時間以上
- **時間帯:**
  - 通常 (9:00-17:00): 平日 ¥3,500/h、土日祝 ¥3,900/h
  - 時間外 (7:00-9:00, 17:00-20:00): 平日 ¥3,800/h、土日祝 ¥4,200/h
  - 早朝夜間 (~7:00, 20:00~): 平日 ¥4,000/h、土日祝 ¥4,400/h
- **兄弟加算:** +¥1,000/h
- **家事代行・沐浴オプション:** ¥1,100/回
- **交通費:** 手入力
- **要件証明:** あり

## 機能一覧
- CSVデータ取込（Shift-JIS/UTF-8対応）
- 児童一覧表示・日付フィルタ
- 登園・降園時刻記録（ボタンタップ）
- 延長料金自動計算（30分単位）
- ケア記録（食事・睡眠・排泄・ミルク等）
  - 同一項目は上書き保存（upsert方式）
  - 記録ボタン押下時に各項目の近くに「✅〇〇を記録しました」と表示
  - 体温①②（小数点1位表示）、排便①②③、お昼寝①②③はインデックス付きで個別管理
  - 昼食時間: 10:00-14:00、おやつ時間: 13:00-16:00（セレクトボックス）
  - 昼食・おやつに内容テキスト入力欄あり（15文字程度）
  - お昼寝は3回分入力可能、時間範囲は06:00-22:00
  - 予約IDごとにsession_stateでデータ保持（タブ切替時も維持）
- 当日キャンセル処理（基本料金50%）
- 領収書PDF発行（日本語対応）
  - 宛名は保護者フルネーム表示
  - PDFメタデータ（タイトル、作成者等）を適切に付与
  - スタッフ選択時に資格取得日・要件が自動記載
- 連絡帳テキスト自動生成
- **料金計算シミュレーター**
  - サービス種別ごとのタブUI
  - 15分刻みの時刻選択
  - 土日祝自動判定（jpholiday使用）
  - 時間帯別料金内訳の詳細表示
  - オプション（兄弟・食事・家事代行等）対応

## 記録表示形式
- 記録内容の時間を先に表示（例：「検温時間 7:15 体温① 36.5°C」）
- システム記録時間は薄いグレーで末尾に表示
- 保護者への連絡帳にはシステム時間は含めない

## 日付フィルター機能
「予約一覧」「実績入力」「領収書発行」の各画面で以下の期間選択が可能:
- **今日/今月**: デフォルト表示
- **日付指定**: カレンダーから特定日を選択
- **月指定**: 年月を選択して月単位で表示

CSV取込時は日付に関わらず全データをデータベースに保存。

## 起動方法
```bash
streamlit run app.py --server.port 5000
```

## データベーステーブル
- `reservations`: 予約データ
- `attendance_records`: 登降園・料金情報
- `care_records`: ケア記録（record_typeに`temperature_1`、`stool_2`のようにインデックス付き）
- `staff`: スタッフ情報（要件証明用）
- `nap_check_logs`: 午睡チェック詳細ログ（5分刻み記録）
- `users`: ユーザー情報（id, username, password_hash, salt, role, display_name）
- `user_sessions`: 永続ログインセッション（user_id, session_token, expires_at）
- `fee_settings`: 料金マスター設定

## 午睡チェック機能（監査対応）
お昼寝記録に「詳細」ボタンを追加。入眠〜起床時間を5分刻みで詳細記録可能。

### データ構造
- **nap_check_logs テーブル**
  - `reservation_id`: 予約ID
  - `nap_index`: 午睡回数（1〜3）
  - `check_time`: チェック時刻（例: 13:00, 13:05...）
  - `arrow_direction`: 姿勢（up=仰向け, down=うつ伏せ, left=左向き, right=右向き）
  - `is_corrected`: 体位修正フラグ（◯マーク）
  - `staff_name`: 担当スタッフ名

### UI仕様
- 開始・終了時間を設定後、「詳細」ボタンが有効化
- 5分刻みの行が自動生成（例: 13:00〜14:00なら13行）
- 各行に4方向の矢印ボタン、体位修正チェックボックス、担当者ドロップダウン
- 担当者は前の行から自動継承
- 全行の担当者が選択されていないと保存不可（監査対応）
- 一括保存・クリア機能

## マルチ施設対応
アプリは2つの施設を切り替えて運用可能。

### 施設
- **こぐまハウス** (facility_id: "house")
- **こぐまbaby** (facility_id: "baby")

### 切り替え方法
- ヘッダー上部のドロップダウンで施設を選択
- 選択した施設のデータのみが各画面に表示される

### データ区分
- reservationsテーブルにfacility_idカラムを追加
- staffテーブルにfacility_idカラム追加（"house"/"baby"/"both"）
- 既存データはデフォルトで「こぐまハウス」に所属
- CSVインポート時は現在選択中の施設にデータが取り込まれる

## 設定画面
サイドバーの「⚙️ 設定」からアクセス可能。

### スタッフ管理
- スタッフの新規登録・編集・削除
- 入力項目: 氏名、氏名（かな）、資格取得日、資格種別、所属施設
- 所属施設: 両方/こぐまハウスのみ/こぐまbabyのみ
- データはSQLiteのstaffテーブルに保存

## ユーザー認証機能
アプリはログイン認証が必要。

### ログイン
- ログインしていないユーザーは全画面にアクセス不可
- ユーザーIDとパスワードで認証
- **永続セッション管理**（Cookie + データベース）

### セッション管理
- **有効期限**: 30日間（ログアウトボタンを押さない限り維持）
- **保存方式**: ブラウザCookie + データベース（user_sessionsテーブル）
- **ログアウト**: 明示的にログアウトボタンを押すとセッション削除
- **自動クリーンアップ**: 期限切れセッションは起動時に自動削除

### 権限（ロール）
- **管理者（admin）**: 全機能＋管理者ダッシュボードにアクセス可能
- **一般スタッフ（user）**: 管理者ダッシュボード以外の全機能にアクセス可能

### 初期ユーザー
- ユーザーID: `admin`
- パスワード: `admin123`
- ※初回ログイン後にパスワード変更を推奨

### パスワード変更
- 設定画面から自分のパスワードを変更可能
- 4文字以上で設定

### 管理者ダッシュボード（管理者のみ）
- サイドバー「🔧 管理者ダッシュボード」からアクセス
- ユーザーの追加・編集・削除が可能
- 既存ユーザーのパスワードリセット機能
- adminユーザーは削除不可
- 料金マスター管理機能（基本料金・オプション・割引設定）
- **セッションログビューワー**: iPad等での意図しないログアウトの原因特定用

### セッションログ機能（デバッグ用）
- `session_logs`テーブルにセッション関連イベントを記録
- イベントタイプ: LOGIN_SUCCESS, LOGOUT, RESTORE_SUCCESS, RESTORE_FAILED, VALIDATE_EXPIRED等
- 管理者ダッシュボードから直近100件のログを確認可能
- iOS Safari特有の問題（Cookie削除、メモリ解放等）の原因追跡に活用

### データベーステーブル
- `users`: ユーザー情報（id, username, password_hash, salt, role, display_name）
- `user_sessions`: 永続ログインセッション（user_id, session_token, expires_at）
- `session_logs`: セッションイベントログ（event_type, user_id, username, session_token_prefix, details）

## 料金計算ルール（2025年4月改定）
- **基本料金**: 全サービス30分単位で切り上げ課金
- **最低利用時間**: 一時預かり1時間、施設型シッター2時間、自宅型シッター3時間
- **延長料金**: 予約時間超過10分以内は無料（グレースタイム）、10分超で30分単位に切り上げ課金
- **施設利用料**: 一時預かり¥550、施設型シッター¥2,200（税込）

## 最終更新
2026年2月17日 - 2025年4月改定料金ルール適用（30分単位課金・延長グレースタイム・最低利用時間）
